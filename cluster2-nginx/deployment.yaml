apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-cluster2
  namespace: dev2
spec:
  replicas: 2
  selector:
    matchLabels:
      app: nginx-cluster2
  template:
    metadata:
      labels:
        app: nginx-cluster2
    spec:
      containers:
      - name: nginx
        image: nginx:latest
        ports:
        - containerPort: 80
